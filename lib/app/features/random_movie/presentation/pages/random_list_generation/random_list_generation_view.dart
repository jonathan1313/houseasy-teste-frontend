import 'package:flutter/material.dart';

class RandomListGenerationView extends StatelessWidget {
  const RandomListGenerationView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
